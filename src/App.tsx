/**
 * Punto de montaje de la aplicación (ver docs/architecture.md, capa 8).
 * Placeholder de la feature `scaffolding`: aquí se montarán el router
 * (`src/routes`), el provider de la librería de fetching y el guard de
 * autenticación (`src/auth`) conforme se implementen esas features.
 */

export function App() {
  return (
    <main>
      <h1>front</h1>
      <p>Scaffolding listo. La UI real llega con las siguientes features.</p>
    </main>
  );
}
