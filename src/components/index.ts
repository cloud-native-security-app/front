/**
 * Componentes de UI puros y reutilizables, sin conocimiento del Gateway
 * (ver docs/architecture.md, capa 6: "src/components"). Un componente por
 * archivo, nombre de archivo = nombre del componente (docs/conventions.md).
 *
 * Placeholder de la feature `scaffolding` (feature_list.json, id 1): los
 * primeros componentes concretos se añaden junto con la feature que los
 * necesite (p. ej. `scan_request_form`, id 4).
 */

export {};
